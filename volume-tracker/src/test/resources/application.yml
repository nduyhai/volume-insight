server:
  port: 8080
  http2:
    enabled: true
spring:
  application:
    name: volume-tracker
  threads:
    virtual:
      enabled: true
  jpa:
    show-sql: false
    open-in-view: false
    hibernate:
      ddl-auto: none
    generate-ddl: false
  datasource:
    url: jdbc:mysql://localhost:3306/volume_db
    username: root
    password: volumepassword

  flyway:
    enabled: false